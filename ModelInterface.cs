public interface ModelInterface
{
    public void Encode(Stream input, Stream output);
    public void Decode(Stream inputm Stream output);

    //public Update(byte bite);
    //public AddSymbol(byte bite);
}